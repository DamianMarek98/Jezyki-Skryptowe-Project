def math_comb(n, k):
    return factorial(n) / (factorial(k) * factorial(n - k))


def factorial(n):
    result = 1
    for i in range(1, n + 1):
        result = result * i

    return result

print('xd')
print(math_comb(10, 4))

